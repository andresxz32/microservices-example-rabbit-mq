#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/home/pipe-dev/dev/personal-projects/microservices-example/api-gateway/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/home/pipe-dev/dev/personal-projects/microservices-example/api-gateway/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../@cucumber+gherkin-streams@5.0.1_d53rl7ibivlicmgekdtetrczwa/node_modules/@cucumber/gherkin-streams/bin/gherkin" "$@"
else
  exec node  "$basedir/../../../../../../@cucumber+gherkin-streams@5.0.1_d53rl7ibivlicmgekdtetrczwa/node_modules/@cucumber/gherkin-streams/bin/gherkin" "$@"
fi
